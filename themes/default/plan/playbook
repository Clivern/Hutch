- name: chestnut plan
  hosts: remote
  become: yes
  vars:
    chestnut_version: "0.2.0"
    chestnut_plan_uuid: "{{ plan_uuid }}"
    {% for var in vars %}
    {{ var.name }}: "{{ var.value }}"
    {% endfor %}
  roles:
    {% for role in roles %}
    - role: ../../plan/{{ role.name }}
    {% endfor %}
