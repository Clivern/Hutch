- name: pilgrim plan
  hosts: remote
  become: yes
  vars:
    pilgrim_version: 0.2.0
    pilgrim_plan_uuid: {{ plan_uuid }}
    {% for var in vars %}{{ var.name }}: {{ var.value }}{% endfor %}
  roles:
    {% for role in roles %}- role: ../../plan/{{ role.name }}{% endfor %}
